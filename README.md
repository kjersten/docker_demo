# README

To get this application up and running, install [Docker For Mac](https://docs.docker.com/docker-for-mac/install/), open up a command line in the application base directory and run:
> docker-compose up --build

Refer to the following files to see how Docker is configured:
* Dockerfile
* docker-compose.yml
* .env
* .dockerignore
